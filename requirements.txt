# RA-OV3DSeg extras (assumes Pointcept env already set up)
numpy==1.26.4
opencv-python-headless==4.8.1.78
nuscenes-devkit==1.1.11
transformers==4.46.0
open-clip-torch==2.24.0
open3d
ftfy
pyyaml
matplotlib
tqdm
Pillow
# SAM2 is intentionally not pinned here: the official package currently wants
# torch>=2.5.1 and can upgrade the Pointcept torch==2.1.x environment.
